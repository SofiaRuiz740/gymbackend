apiVersion: apps/v1
kind: Deployment
metadata:
  name: category-service
  namespace: gym-inventory
spec:
  replicas: 1
  selector:
    matchLabels:
      app: category-service
  template:
    metadata:
      labels:
        app: category-service
    spec:
      containers:
        - name: category-service
          image: us-central1-docker.pkg.dev/gym-inventory-platform/gym-backend/category-service:v2
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: DB_NAME
              value: gym_category_db
            - name: DB_SCHEMA
              value: catalog
          envFrom:
            - configMapRef:
                name: platform-config
            - secretRef:
                name: platform-secret

          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080   # 🔥 CORREGIDO
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6

          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080   # 🔥 CORREGIDO
            initialDelaySeconds: 90
            periodSeconds: 20

---
apiVersion: v1
kind: Service
metadata:
  name: category-service
  namespace: gym-inventory
spec:
  selector:
    app: category-service
  ports:
    - port: 8080
      targetPort: 8080