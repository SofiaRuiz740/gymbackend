apiVersion: apps/v1
kind: Deployment
metadata:
  name: report-service
  namespace: gym-inventory
spec:
  replicas: 1
  selector:
    matchLabels:
      app: report-service
  template:
    metadata:
      labels:
        app: report-service
    spec:
      containers:
        - name: report-service
          image: us-central1-docker.pkg.dev/gym-inventory-platform/gym-backend/report-service:v1
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: DB_NAME
              value: gym_report_db
            - name: DB_SCHEMA
              value: reporting
          envFrom:
            - configMapRef:
                name: platform-config
            - secretRef:
                name: platform-secret

          # 🔥 FIX
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6

          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 90
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: report-service
  namespace: gym-inventory
spec:
  selector:
    app: report-service
  ports:
    - port: 8080
      targetPort: 8080